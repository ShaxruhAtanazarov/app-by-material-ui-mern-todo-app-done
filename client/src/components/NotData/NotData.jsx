import React from "react";

// importing styles
import "./NotData.scss";

const NotData = () => {
  return (
    <div
      style={{
        height: "200px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "40px",
      }}
    >
      Add To Do...
    </div>
  );
};

export default NotData;
